<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="production_mode_login_form" inherit_id="web.login" priority="90">
        <xpath expr="//div[@class='mb-3'][label[@for='db']]" position="attributes">
            <attribute name="t-if">
                databases
                and len(databases) &gt; 1
                and not (
                    request.db
                    and str(request.env['ir.config_parameter'].sudo().get_param('production_mode.enabled', default='False')).strip().lower() in ('1', 'true', 'yes', 'on')
                )
            </attribute>
        </xpath>
    </template>
</odoo>
